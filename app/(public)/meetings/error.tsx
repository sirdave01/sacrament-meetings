"use client";

// Share the reset-and-return experience with the admin meetings boundary.
import MeetingsError from "@/components/MeetingsError";

// Attach the shared client boundary to public meeting routes.
export default MeetingsError;